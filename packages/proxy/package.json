{
  "name": "proxy",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "nodemon -e ts -w src --exec \"yarn build && yarn start\"",
    "build": "tsc --build",
    "start": "node dist"
  },
  "dependencies": {
    "cors-anywhere": "^0.4.4"
  },
  "devDependencies": {
    "nodemon": "^2.0.12",
    "typescript": "^4.3.5"
  }
}