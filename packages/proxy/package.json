{
  "name": "proxy",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "nodemon -e ts -w src --exec \"yarn build && yarn start\"",
    "build": "tsc --build",
    "start": "node dist",
    "deploy": "git push heroku main -f"
  },
  "dependencies": {
    "cors-anywhere": "^0.4.4"
  },
  "devDependencies": {
    "@types/node": "^16.7.2",
    "nodemon": "^2.0.12",
    "typescript": "^4.3.5"
  },
  "engines": {
    "node": "^14.16.0",
    "yarn": "^1.22.5"
  }
}